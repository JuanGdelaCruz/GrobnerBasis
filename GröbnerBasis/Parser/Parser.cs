using GröbnerBasis.PolynomialRings;
using System.Text.RegularExpressions;
namespace GröbnerBasis.Parser
{
    public static class Parser
    {


        //public static Polynomial Parse(string pstring)
        //{
        //    Polynomial parsed = new Polynomial();
        //    //First we remove all the white spaces.
        //    pstring = Regex.Replace(pstring, @"s", "");
        //    //Then we separate the string into terms
        //    string[] terms = Regex.Split(pstring, @"(?=[\+-])");

        //    return parsed;
        //}

        //private static Term ParseTerm(string term)
        //{
        //    Term parsedTerm;
        //    double coef = 0;
        //    int sign = (term[0] == '-') ? -1 : 1;


        //    return null;
        //}

    }
}
